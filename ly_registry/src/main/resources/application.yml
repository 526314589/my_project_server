# 注册中心端口号
server:
  port: 10086
#  注册中心 service_id
spring:
  application:
    name: ly_registry
#eurkea 注册
eureka:
  client:
    #拉取
    fetch-registry: false
    #注册
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
